{
  "name": "bagabayan-site",
  "version": "1.0.0",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "build": "rm -rf public && mkdir -p public && cp index.html public/ || true && cp -R css js pages assets public/ || true"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0"
  }
}
